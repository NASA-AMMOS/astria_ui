#### Find images from a single sol

To find a specific image by filename, enter the image filename into the filename filter in the `Sol` tab or enter the filename into the main input in the `Search` tab.
